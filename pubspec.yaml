name: mothereye
description: MotherEye - Child safety app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.1.0
  cloud_firestore: ^5.1.0
  firebase_messaging: ^15.0.0
  shared_preferences: ^2.2.0
  permission_handler: ^11.3.0
  intl: ^0.19.0
  uuid: ^4.3.3
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
